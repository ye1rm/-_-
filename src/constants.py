# constants.py
import pygame

# 고정 화면 크기
CELL_SIZE = 20
GRID_WIDTH = 40
GRID_HEIGHT = 40
WIDTH = CELL_SIZE * GRID_WIDTH
HEIGHT = CELL_SIZE * GRID_HEIGHT

# 색상
BLACK = (0, 0, 0)

# 상태 정의
STATE_HOME = "home"
